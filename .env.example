# Anthropic API Key - replace with your actual key
ANTHROPIC_API_KEY=

# Google Gemini API Key - used as a fallback if the Anthropic API fails
GEMINI_API_KEY=

# Server port (optional, defaults to 3000)
PORT=3000
